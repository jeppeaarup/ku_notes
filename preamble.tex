% --- Flags used for conditional compilation ---
\usepackage{ifthen} % For if-then-else logic in LaTeX
\newboolean{usechapters}

\setboolean{usechapters}{true} % true = use chapters instead of sections in counters, headers etc. (if the class supports it)

% --- Settings in external files ---
\input{settings/colors.tex}
\input{settings/layout.tex}
\input{settings/fonts.tex}
\input{settings/figures_tables.tex}

% --- Math ---
\usepackage{amsmath} % For math stuff
\usepackage{amssymb} % For math symbols
\usepackage{nicematrix} % For creating nice matrices
\usepackage{bm} % For bold math symbols

% --- Citing and referencing ---
\usepackage{biblatex} % For bibliography
\usepackage{appendix} % For setting up appendix titles
\usepackage{hyperref} % Hyperrefs in TOC, cites, refs etc.
\usepackage[nameinlink, noabbrev]{cleveref} % For better crossrefs

\addbibresource{references.bib}

\hypersetup{ % Set hyperref link colors
    colorlinks,
    citecolor=black,  
    filecolor=black,
    linkcolor=black,
    urlcolor=blue
    }

\urlstyle{same} % Set the URL-font to the same as the rest of the document

% --- Color boxes --- 
\input{settings/boxes.tex} % Needs to come after hyperref

% --- Misc packages ---
\usepackage[style=ddmmyyyy]{datetime2} % Generates todays date
\usepackage{lipsum} % Generates lorem ipsum tex
\usepackage{transparent} % For transparent text effects