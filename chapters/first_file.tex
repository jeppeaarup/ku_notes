\section{Header 1}
\lipsum[1]

\subsection{Header 2}
\lipsum[2]

\subsection{Header 3}
\lipsum[3]

\section{Boxes}

This template includes boxes for definitions (\cref{def:gaussmarkov}), theorems (\cref{thm:gaussmarkov}), methods (\cref{mth:gaussmarkov}), examples (\cref{exa:gaussmarkov}), highlights (\cref{box:gaussmarkov}), and proofs (\cref{prf:gaussmarkov}). Furthermore, there is an important box for emphasizing key information - but that one is unnumbered.

\begin{definition}{Gauss-Markov}{gaussmarkov}

In a linear regression model $Y = X\beta + \varepsilon$ with assumptions:
\begin{itemize}
    \item $\mathbb{E}[\varepsilon] = 0$ (zero mean errors)
    \item $\text{Var}(\varepsilon) = \sigma^2 I$ (homoscedasticity and no autocorrelation)
    \item $X$ is full rank
\end{itemize}
the ordinary least squares (OLS) estimator $\hat{\beta} = (X^TX)^{-1}X^TY$ 
is the Best Linear Unbiased Estimator (BLUE), meaning it has the smallest 
variance among all linear unbiased estimators.

\end{definition}

\begin{theorem}{Gauss-Markov}{gaussmarkov}

In a linear regression model $Y = X\beta + \varepsilon$ with assumptions:
\begin{itemize}
    \item $\mathbb{E}[\varepsilon] = 0$ (zero mean errors)
    \item $\text{Var}(\varepsilon) = \sigma^2 I$ (homoscedasticity and no autocorrelation)
    \item $X$ is full rank
\end{itemize}
the ordinary least squares (OLS) estimator $\hat{\beta} = (X^TX)^{-1}X^TY$ 
is the Best Linear Unbiased Estimator (BLUE), meaning it has the smallest 
variance among all linear unbiased estimators.

\end{theorem}

\begin{method}{Gauss-Markov}{gaussmarkov}

    In a linear regression model $Y = X\beta + \varepsilon$ with assumptions:
\begin{itemize}
    \item $\mathbb{E}[\varepsilon] = 0$ (zero mean errors)
    \item $\text{Var}(\varepsilon) = \sigma^2 I$ (homoscedasticity and no autocorrelation)
    \item $X$ is full rank
\end{itemize}
the ordinary least squares (OLS) estimator $\hat{\beta} = (X^TX)^{-1}X^TY$ 
is the Best Linear Unbiased Estimator (BLUE), meaning it has the smallest 
variance among all linear unbiased estimators.
    
\end{method}

\begin{example}{Gauss-Markov}{gaussmarkov}

In a linear regression model $Y = X\beta + \varepsilon$ with assumptions:
\begin{itemize}
    \item $\mathbb{E}[\varepsilon] = 0$ (zero mean errors)
    \item $\text{Var}(\varepsilon) = \sigma^2 I$ (homoscedasticity and no autocorrelation)
    \item $X$ is full rank
\end{itemize}
the ordinary least squares (OLS) estimator $\hat{\beta} = (X^TX)^{-1}X^TY$ 
is the Best Linear Unbiased Estimator (BLUE), meaning it has the smallest 
variance among all linear unbiased estimators.

\end{example}

\begin{highlight}{Gauss-Markov}{gaussmarkov}

In a linear regression model $Y = X\beta + \varepsilon$ with assumptions:
\begin{itemize}
    \item $\mathbb{E}[\varepsilon] = 0$ (zero mean errors)
    \item $\text{Var}(\varepsilon) = \sigma^2 I$ (homoscedasticity and no autocorrelation)
    \item $X$ is full rank
\end{itemize}
the ordinary least squares (OLS) estimator $\hat{\beta} = (X^TX)^{-1}X^TY$ 
is the Best Linear Unbiased Estimator (BLUE), meaning it has the smallest 
variance among all linear unbiased estimators.

\end{highlight}


\begin{proof}{Gauss-Markov}{gaussmarkov}

In a linear regression model $Y = X\beta + \varepsilon$ with assumptions:
\begin{itemize}
    \item $\mathbb{E}[\varepsilon] = 0$ (zero mean errors)
    \item $\text{Var}(\varepsilon) = \sigma^2 I$ (homoscedasticity and no autocorrelation)
    \item $X$ is full rank
\end{itemize}
the ordinary least squares (OLS) estimator $\hat{\beta} = (X^TX)^{-1}X^TY$ 
is the Best Linear Unbiased Estimator (BLUE), meaning it has the smallest 
variance among all linear unbiased estimators.

\end{proof}


\begin{important}
    Lorem ipsum dolor sit amet, consectetuer adipiscing elit. Ut purus elit, vestibulum ut, placerat ac, adipiscing vitae, felis.
\end{important}